plugins:
  - name: solidity

dependencies:
  - name: dsnote
    local: ../
    # TODO: Can remove line below once ape #861 is released
    contracts_folder: src

solidity:
  import_remapping:
    - "@dsnote=dsnote"
